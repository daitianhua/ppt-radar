# 网页固定地址申请指南（针对 ppt-radar 自动资讯站）

> 适用场景：你有一个每天自动更新资讯的静态网站（ppt-radar，靠 `git push` 刷新 `news.js`），
> 想给它一个**固定、能随时打开**的访问地址。
> 当前临时地址在 `current_url.txt`：`https://...sh1.agentos-app.net`（哈希前缀，不好记、可能随重新部署变化）。

---

## 一、先想清楚："固定"有两种含义

| 你想要什么 | 说明 | 成本 | 国内访问 |
|---|---|---|---|
| **A. 不变的可访问地址** | 平台给一个永久固定的子域名，如 `xxx.github.io/ppt-radar` | 免费 | 看平台（GitHub 偶尔慢，Cloudflare/Vercel 稍好） |
| **B. 好记的自己的域名** | 如 `ppt.你的名字.com`，完全自定义 | 域名费约 30–80 元/年 + 国内节点需备案 | 快（配国内节点） |

自用 + 自动更新，优先选 **A** 立刻拿到固定地址；想要好记/正式再上 **B**。

---

## 二、方案对比（静态站，零后端，完美对接你的 `git push`）

| 方案 | 固定地址样子 | 自动更新怎么对接 | 国内速度 | 备注 |
|---|---|---|---|---|
| **GitHub Pages** | `https://你的用户名.github.io/ppt-radar` | 你现有的 `git push` 即可，零改动 | 一般，偶尔慢 | 最成熟，README 已规划 |
| **Cloudflare Pages** | `https://ppt-radar.pages.dev`（可自定义） | 连 GitHub 仓库，push 自动重新部署 | 较好 | 免费，国内比 GitHub 稳 |
| **Vercel** | `https://ppt-radar.vercel.app` | 连 GitHub 仓库，push 自动部署 | 较好 | 免费额度够用 |
| **EdgeOne Pages（腾讯云）** | 平台分配固定子域名 | 连 Git 仓库，push 自动部署 | 快（腾讯系） | 国内访问最佳，细节以控制台为准 |

**结论**：你已经在用 `git push` 自动更新，上面任何一个"连 GitHub 仓库 → push 即重新部署"的方案都能**不改任何逻辑**直接拿到固定地址。

---

## 三、最快拿到固定地址：GitHub Pages（零成本、零改动）

前提：你的 ppt-radar 已经在某个 GitHub 仓库里（README 写的就是它）。

1. 打开仓库 → **Settings → Pages**。
2. Source 选 **Deploy from a branch**，分支选 `main`（或 `master`），目录选 `/ (root)`。
3. 保存，等 1–2 分钟。
4. 固定地址就是：`https://你的用户名.github.io/ppt-radar`

之后你的自动化任务每天 `git push`，GitHub Pages 几分钟内自动刷新——**和现在一模一样，只是地址固定了**。
把 `README.md` 第 8 行的占位符换成真实用户名即可。

> 唯一缺点：国内有时候打开慢。能接受就将就用；不能接受看方案四。

---

## 四、国内访问更快：Cloudflare Pages / Vercel / EdgeOne Pages

以 Cloudflare Pages 为例（其余同理）：

1. 注册 Cloudflare，进 **Pages** → **Create a project** → **Connect to Git**。
2. 授权并选 ppt-radar 仓库。
3. 构建设置：框架选 `None` / 直接部署，因为你是纯静态 HTML（不需要 build 命令）。
4. 部署完成，得到固定地址 `https://ppt-radar.pages.dev`，**永不变**。
5. 之后每次 `git push`，Cloudflare 自动拉最新代码重新部署，页面即更新。

Vercel、EdgeOne Pages 步骤几乎一样，都是"连仓库 → 自动部署"。

---

## 五、想要自己的域名（好记 + 国内快）——进阶

如果你想要 `ppt.你的名字.com` 这种：

1. **买域名**：阿里云 / 腾讯云 / Cloudflare 搜一个没被注册的，约 30–80 元/年。
2. **备案（关键）**：只要用**国内节点**对外提供网页，自定义域名必须做 ICP 备案（腾讯云/阿里云有免费备案引导，约 1–2 周）。
   - 用 GitHub Pages / Cloudflare Pages 默认境外节点 + 自定义域名 → **不用备案**，但国内速度一般。
   - 用 EdgeOne / 腾讯云 COS 国内节点 + 自定义域名 → **必须备案**，但国内飞快。
3. **绑定**：在托管平台（如 EdgeOne / Cloudflare）添加自定义域名，按提示去域名商后台加一条 **CNAME** 解析。
4. 完成：拿到完全固定、好记、可自定义的地址。

> 自用、不对外推广，**方案 A 已经够**；只有"想给别人发一个好记的链接"或"要快"才值得走 B。

---

## 六、对接你现在的自动更新（重点）

你现在的流程不需要动：

```
自动化任务每天 08:30
  → 搜索当天 PPT 资讯
  → prepend 到 news.js
  → git commit && git push
        ↓
  托管平台检测到 push → 自动重新部署 → 固定地址页面刷新
```

无论选 GitHub Pages / Cloudflare / Vercel / EdgeOne，**只要把仓库连上去，push 就刷新**，你现有的 `news.js` 更新逻辑、定时任务全部照旧。

---

## 七、建议你怎么做（按成本从低到高）

1. **今天就做**：GitHub Pages 或 Cloudflare Pages，5 分钟拿到固定地址，立刻替换 `current_url.txt` 里的临时哈希地址。
2. **要快/要正式**：上 EdgeOne Pages（国内快）或买个自定义域名（需备案）。
3. **别做**：为了"固定地址"去租云服务器自己搭 Nginx——你这是纯静态站，静态托管免费又快，杀鸡不用牛刀。

---

需要的话，我可以直接帮你把 ppt-radar 部署到 Cloudflare Pages / EdgeOne Pages 并拿到固定地址，你确认用哪个平台我就动手。
